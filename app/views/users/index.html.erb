<table>

  <% if @users.nil? %>
      <tr>
        <td>No users</td>
      </tr>
  <% else %>
      <% @users.each do |user| %>
          <tr>
            <td><%= user.login %></td>
            <td><%= link_to 'Show', user_path(user) %></td>
          </tr>
      <% end %>
  <% end %>

</table>